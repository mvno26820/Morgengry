using System;
using System.Collections.Generic;
using System.Text;

namespace Morgengry
{
    // enums is making its own class with its own data type so it can be placed under namespace
    public enum Level
    {
        low, medium, high
    }

    class Enum
    {
    }
}
